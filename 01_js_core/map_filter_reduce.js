// Implement a method Array.prototype.myMap() without using the native map() function.
Array.prototype.myMap = function (callback) {
  // implement logic
};

const result = [1, 2, 3].myMap((x) => x * 2);
console.log(result); // [2, 4, 6]
